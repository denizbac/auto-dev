apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: auto-dev-postgres
  namespace: autodev
  labels:
    team: ois
    costcenter: "14866"
spec:
  serviceName: auto-dev-postgres
  replicas: 1
  selector:
    matchLabels:
      app: auto-dev-postgres
  template:
    metadata:
      labels:
        app: auto-dev-postgres
        team: ois
        costcenter: "14866"
    spec:
      imagePullSecrets:
        - name: auto-dev-registry-cred
      containers:
        - name: postgres
          image: registry.nimbus.amgen.com/dbac/autodev/postgres:16
          resources:
            requests:
              cpu: "500m"
              memory: "1Gi"
            limits:
              cpu: "2"
              memory: "4Gi"
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: autodev
            - name: POSTGRES_USER
              value: autodev
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: auto-dev-secrets
                  key: DB_PASSWORD
          volumeMounts:
            - name: postgres-data
              mountPath: /var/lib/postgresql/data
  volumeClaimTemplates:
    - metadata:
        name: postgres-data
      spec:
        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: 20Gi
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: auto-dev-redis
  namespace: autodev
  labels:
    team: ois
    costcenter: "14866"
spec:
  serviceName: auto-dev-redis
  replicas: 1
  selector:
    matchLabels:
      app: auto-dev-redis
  template:
    metadata:
      labels:
        app: auto-dev-redis
        team: ois
        costcenter: "14866"
    spec:
      imagePullSecrets:
        - name: auto-dev-registry-cred
      containers:
        - name: redis
          image: registry.nimbus.amgen.com/dbac/autodev/redis:7
          resources:
            requests:
              cpu: "200m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          ports:
            - containerPort: 6379
          volumeMounts:
            - name: redis-data
              mountPath: /data
  volumeClaimTemplates:
    - metadata:
        name: redis-data
      spec:
        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: 10Gi
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: auto-dev-qdrant
  namespace: autodev
  labels:
    team: ois
    costcenter: "14866"
spec:
  serviceName: auto-dev-qdrant
  replicas: 1
  selector:
    matchLabels:
      app: auto-dev-qdrant
  template:
    metadata:
      labels:
        app: auto-dev-qdrant
        team: ois
        costcenter: "14866"
    spec:
      imagePullSecrets:
        - name: auto-dev-registry-cred
      containers:
        - name: qdrant
          image: registry.nimbus.amgen.com/dbac/autodev/qdrant:latest
          resources:
            requests:
              cpu: "500m"
              memory: "1Gi"
            limits:
              cpu: "2"
              memory: "4Gi"
          ports:
            - containerPort: 6333
          volumeMounts:
            - name: qdrant-data
              mountPath: /qdrant/storage
  volumeClaimTemplates:
    - metadata:
        name: qdrant-data
      spec:
        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: 20Gi
