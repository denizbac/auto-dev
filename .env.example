# Auto-Dev Environment Configuration
# ====================================
# Copy to .env and fill in values:
#   cp .env.example .env
#   nano .env

# =============================================================================
# Database
# =============================================================================
DB_HOST=localhost
DB_USER=autodev
DB_PASSWORD=change_this_secure_password
DB_NAME=autodev

# =============================================================================
# Redis & Qdrant
# =============================================================================
REDIS_URL=redis://localhost:6379
QDRANT_HOST=localhost

# =============================================================================
# GitLab Integration
# =============================================================================
# Personal Access Token with api, read_repository, write_repository scopes
GITLAB_TOKEN=glpat-xxxxxxxxxxxxxxxxxxxx

# Webhook secret for verifying incoming webhooks
GITLAB_WEBHOOK_SECRET=your_random_webhook_secret

# =============================================================================
# LLM Providers
# =============================================================================
# Primary provider: codex or claude
AUTODEV_LLM_PROVIDER=codex

# Codex (OpenAI) API Key
CODEX_API_KEY=sk-xxxxxxxxxxxxxxxxxxxx

# Anthropic API Key (for Claude fallback)
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxx

# =============================================================================
# Optional: Development Tools
# =============================================================================
# pgAdmin password (for --profile dev)
PGADMIN_PASSWORD=admin

# CLI paths if not in standard location
# CODEX_PATH=/usr/local/bin/codex
# CLAUDE_PATH=/usr/local/bin/claude
